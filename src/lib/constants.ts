export const SPLASH_DURATION_MS = 300000;
export const OVERLAY_DURATION_MS = 3000;
export const DUMMY_SPLASH_INTERVAL_MS = 15000; // Try to add a dummy splash every 15s if idle
export const IDLE_TIME_BEFORE_DUMMY_MS = 30000; // Only start dummy splashes after 30s of silence
